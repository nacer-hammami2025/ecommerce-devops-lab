name: Full DevOps Pipeline

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  # =====================================
  # 1. TERRAFORM INFRASTRUCTURE
  # =====================================
  terraform:
    name: Provision Infrastructure
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform
    outputs:
      instance_ips: ${{ steps.export.outputs.instance_ips }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: latest

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan

      - name: Export Instance IPs
        id: export
        run: |
          IPS=$(terraform output -json instance_public_ips | jq -r '.[]' | tr '\n' ',' )
          echo "instance_ips=$IPS" >> $GITHUB_OUTPUT
          echo "Exported IPs: $IPS"

  # =====================================
  # 2. ANSIBLE CONFIGURATION
  # =====================================
  ansible:
    name: Configure Servers
    runs-on: ubuntu-latest
    needs: terraform
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Ansible & Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ansible jq openssh-client

      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_KEY }}" > ~/.ssh/ec2_key.pem
          chmod 600 ~/.ssh/ec2_key.pem
          ls -la ~/.ssh/

      - name: Generate Inventory
        run: |
          echo "[web]" > inventory.ini
          echo "" >> inventory.ini
          for ip in $(echo "${{ needs.terraform.outputs.instance_ips }}" | tr ',' ' '); do
            if [ ! -z "$ip" ]; then
              echo "$ip ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/ec2_key.pem ansible_python_interpreter=/usr/bin/python3" >> inventory.ini
            fi
          done
          echo "" >> inventory.ini
          echo "[all:vars]" >> inventory.ini
          echo "ansible_host_key_checking=False" >> inventory.ini
          cat inventory.ini

      - name: Run Ansible Playbook - Deploy
        run: |
          ansible-playbook -i inventory.ini ../ansible/deploy.yml -v
        env:
          ANSIBLE_HOST_KEY_CHECKING: "False"

      - name: Verify Deployment
        run: |
          echo "Waiting for services to be ready..."
          sleep 10
          for ip in $(echo "${{ needs.terraform.outputs.instance_ips }}" | tr ',' ' '); do
            if [ ! -z "$ip" ]; then
              echo "Testing connectivity to $ip..."
              curl -s -o /dev/null -w "%{http_code}" http://$ip:80 || echo "Service not yet ready"
            fi
          done

  # =====================================
  # 3. OPTIONAL: TERRAFORM DESTROY
  # =====================================
  destroy:
    name: Destroy Infrastructure (Manual)
    if: github.event_name == 'workflow_dispatch' && contains(github.event.inputs.action, 'destroy')
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Terraform Destroy
        run: terraform destroy -auto-approve
